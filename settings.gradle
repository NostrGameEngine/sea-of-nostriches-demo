
 
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("https://teavm.org/maven/repository") }
        mavenLocal()
    }
    plugins {
        id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
        id("com.android.application") version '8.12.2' apply false
        id("com.android.library") version '8.12.2' apply false
        id("org.jetbrains.kotlin.android") version "2.1.20" apply false
    }
}



plugins {
    id("org.gradle.toolchains.foojay-resolver") version "1.0.0"
}

toolchainManagement {
    jvm {
        javaRepositories {
            repository("foojay") {
                resolverClass = org.gradle.toolchains.foojay.FoojayToolchainResolver
            }
        }
    }
}

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        google()
        maven {
            url = uri("https://central.sonatype.com/repository/maven-snapshots")
        }
        maven { url = uri("https://teavm.org/maven/repository") }

    }
}

def localPropsFile = file("gradle-local.properties")
def localProps = new Properties()

if (localPropsFile.exists()) {
    localPropsFile.withInputStream { localProps.load(it) }
}

// def ngengineDevDir = localProps.getProperty("ngengineDevDir")
// if (ngengineDevDir != null) {
//     println "Including ngengine from $ngengineDevDir"
//     includeBuild(ngengineDevDir) {
//         dependencySubstitution {
//             all { dep ->
//                 if (dep.requested instanceof ModuleComponentSelector &&
//                     dep.requested.group == "org.ngengine" &&
//                     dep.requested.module.startsWith("nge") &&
//                     !dep.requested.module.startsWith("nge-platform-")                    
//                     ) {
//                     dep.useTarget(project(":${dep.requested.module}"))
//                 }
//             }
//         }
//     }
// } 

// def ngePlatformsDevDir = localProps.getProperty("ngePlatformsDevDir")
// if (ngePlatformsDevDir != null) {
//     println "Including nge-platform from $ngePlatformsDevDir"
//     includeBuild(ngePlatformsDevDir) {
//         dependencySubstitution {
//             all { dep ->
//                 if (dep.requested instanceof ModuleComponentSelector &&
//                     dep.requested.group == "org.ngengine" &&
//                     dep.requested.module.startsWith("nge-platform-")                    
//                     ) {
//                     dep.useTarget(project(":${dep.requested.module}"))
//                 }
//             }
//         }
//     }
// } 



 
rootProject.name = settings.appName
include('app')

if(withDesktopPlatform) {
    include('platform-desktop')
}

// if (withAndroidPlatform) {
//     include('platform-android')
// }

// if(withWebPlatform || withIOSCapacitorPlatform){
    include('platform-web')
// }

// if(withIOSCapacitorPlatform){
    include('platform-capacitor')
// }